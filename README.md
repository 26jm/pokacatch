# Poka-Catch

경량 JavaScript 공동구매 데모입니다.

```text
frontend/   # HTML, CSS, JavaScript UI
backend/    # Node.js API
nouse/      # 현재 실행 경로에서 사용하지 않는 이전 구현 및 자료
```

API 서버 실행:

```powershell
cd backend
npm start
```

프런트엔드는 `frontend/index.html`을 브라우저에서 열어 확인할 수 있습니다.
